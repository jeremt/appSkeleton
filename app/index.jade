extends /partials/layout.jade
block content
  section#home(ng-controller="HomeCtrl")
    p.
      Simple skeleton of a web application which use feature such as:
    ul
      li(ng-repeat="feature in features") {{feature}}
    p 
      | Using the simple but powerful #[a(href="http://gulpjs.com") gulp ]
      | build system.